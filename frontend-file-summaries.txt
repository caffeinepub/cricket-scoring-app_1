{
  "frontend/src/pages/LiveScoring.tsx": "Live scoring page resolving matchId from path or search params; fixed to call `useMatchState` with 3 args, use correct `MatchStateResult` properties, and pass `onClose` (not `onOpenChange`) to WicketModal/EndOfOverModal/EndOfInningsModal.",
  "frontend/src/pages/Scorecard.tsx": "Full scorecard page resolving matchId from path or search params, computing batting and bowling stats from ball-by-ball records, with loading/error states and innings breakdown.",
  "frontend/src/pages/MatchHistory.tsx": "Match history page using `getStoredMatches`/`StoredMatch` from matchStore (replacing the non-existent `getAllMatchMetadata`), with correct `ResetAllDataModal` props (`onConfirm`/`onCancel` instead of `onOpenChange`).",
  "frontend/src/pages/Rules.tsx": "Cricket rules reference page with Standard and Tournament tabs; uses Accordion for rule categories, shows loading/error states for tournament rules, and has a Configure button.",
  "frontend/src/data/cricketRules.ts": "Contains the static `cricketRules` array (unchanged) for standard cricket rules, plus a new `getTournamentRules(config: TournamentRules)` function that generates dynamic, config-driven tournament rule categories with template-literal interpolation of all configurable values.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for teams, matches, tournament rules, and reset operations; all queries use enabled guards to prevent running when actor is uninitialized.",
  "frontend/src/components/ConfigureTournamentRulesModal.tsx": "A Dialog modal with a scrollable form for editing all TournamentRules fields — number inputs for numeric parameters, Switch toggles for LBW and Free Hit, pre-filled from backend data with sensible defaults, and a Save button that calls `updateTournamentRules()` with a success toast.",
  "frontend/src/App.tsx": "Main app router using TanStack Router with all routes (/, /history, /teams, /setup, /match/$matchId, /live, /scorecard/$matchId, /scorecard, /rules) registered under a root route that wraps everything in QueryClientProvider and Layout with Outlet.",
  "frontend/src/components/Layout.tsx": "App shell that renders a sticky header, main content area via Outlet (or children prop fallback), BottomTabNavigation, and a footer with caffeine.ai attribution.",
  "frontend/src/components/BottomTabNavigation.tsx": "Fixed bottom navigation bar with four tabs (History, Teams, Setup, Rules) using TanStack Router Link components with correct route paths and active state detection.",
  "frontend/src/pages/MatchSetup.tsx": "Four-step match setup wizard fixed to call `saveMatchMeta(matchId, meta)` with two arguments, `storeMatch(StoredMatch)` with one argument, and `PlayingElevenSelector` with `players`/`selected`/`onChange` props.",
  "frontend/src/components/PlayingElevenSelector.tsx": "New reusable component that displays a team's full squad as selectable checkboxes with a live counter (e.g., \"7 / 11 selected\"), enforces a maximum of 11 selections, and shows visual feedback for selected/disabled/complete states.",
  "frontend/src/lib/matchStore.ts": "Unified match store with localStorage helpers; adds three new utility functions — `clearAllMatches()`, `clearAllMatchMetadata()`, and `clearAllLiveMatchStates()` — that remove all keys managed by the store for use during a full data reset.",
  "frontend/src/components/EndOfOverModal.tsx": "End-of-over modal that accepts bowlingTeamPlayers as an array of playing 11 player IDs, filters all team players to only those in the playing 11, and excludes the current bowler from the next-bowler dropdown.",
  "frontend/src/components/WicketModal.tsx": "Wicket modal that accepts battingTeamPlayers as an array of playing 11 player IDs, filters available next batsmen to only those in the playing 11 (excluding the dismissed batsman).",
  "frontend/src/components/EndOfInningsModal.tsx": "End-of-innings modal that accepts battingTeamPlayers and bowlingTeamPlayers as playing 11 ID arrays, filters all player dropdowns to only the respective team's playing 11 for the second innings opener selection.",
  "frontend/src/hooks/useMatchState.ts": "Custom hook managing live match state (striker/non-striker/bowler IDs, stats, over/innings completion) with localStorage persistence; fixed to import `useMatch` instead of the removed `useGetMatch`.",
  "frontend/src/components/ResetAllDataModal.tsx": "Confirmation AlertDialog modal for the \"Reset All Data\" action; displays a warning icon, a bulleted list of what will be deleted, and two actions — Cancel and a destructive \"Delete All Data\" button with an inline spinner while the reset is in progress.",
  "frontend/src/pages/Teams.tsx": "Teams management page fixed to pass the required `currentPlayers` prop to `BulkPlayerUploadModal` by storing the full `Team` object instead of just the team ID.",
  "frontend/src/components/BulkPlayerUploadModal.tsx": "Three-step modal (input → preview → result) for bulk-adding players to a team via pasted text or .txt/.csv file upload; validates names for length, spaces, and duplicates, shows a preview list with skip reasons, then adds all valid players via the addPlayer mutation and displays a summary — the 11-player cap has been removed."
}