{
  "frontend/src/pages/LiveScoring.tsx": "Live scoring page with all component prop names corrected to match actual interfaces — ScoreDisplay uses `runs`/`overs`/`oversLimit`/`innings`, BatsmanStatsPanel/BowlerStatsPanel use `players`, WicketModal uses `dismissedBatsmanId`/`battingTeamPlayers`/`allPlayers`, EndOfOverModal uses `isOpen`/`onClose`/`bowlingTeamPlayers`/`allPlayers`/`overNumber`, and EndOfInningsModal uses `isOpen`/`onClose`/`inningsNumber`/`battingTeamPlayers`/`bowlingTeamPlayers`/`allPlayers`/`score`.",
  "frontend/src/pages/Scorecard.tsx": "Full scorecard page updated to destructure error/refetch from all four backend queries (match, teams, innings1, innings2) and show a QueryErrorState with a combined retry callback when any query fails, plus a loading skeleton state.",
  "frontend/src/pages/MatchHistory.tsx": "Match history page showing stored matches with continue/scorecard links; uses `createdAt` (number timestamp) instead of the removed `date` field on `StoredMatch`, and displays the match result string when available.",
  "frontend/src/pages/Rules.tsx": "Cricket rules page updated to use QueryErrorState for the tournament rules error state in the Tournament Rules tab, replacing the raw error message with a friendly description and Retry button.",
  "frontend/src/data/cricketRules.ts": "Contains the static `cricketRules` array (unchanged) for standard cricket rules, plus a new `getTournamentRules(config: TournamentRules)` function that generates dynamic, config-driven tournament rule categories with template-literal interpolation of all configurable values.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for teams, matches, deliveries, tournament rules, and reset; imports the correct clear utilities (`clearAllMeta`, `clearAllLocalState`, `clearAllMatchState`) from matchStore instead of the old non-existent names.",
  "frontend/src/components/ConfigureTournamentRulesModal.tsx": "A Dialog modal with a scrollable form for editing all TournamentRules fields — number inputs for numeric parameters, Switch toggles for LBW and Free Hit, pre-filled from backend data with sensible defaults, and a Save button that calls `updateTournamentRules()` with a success toast.",
  "frontend/src/App.tsx": "Main app router using TanStack Router with all routes (/, /history, /teams, /setup, /match/$matchId, /live, /scorecard/$matchId, /scorecard, /rules) registered under a root route that wraps everything in QueryClientProvider and Layout with Outlet.",
  "frontend/src/components/Layout.tsx": "App shell that renders a sticky header, main content area via Outlet (or children prop fallback), BottomTabNavigation, and a footer with caffeine.ai attribution.",
  "frontend/src/components/BottomTabNavigation.tsx": "Fixed bottom navigation bar with four tabs (History, Teams, Setup, Rules) using TanStack Router Link components with correct route paths and active state detection.",
  "frontend/src/pages/MatchSetup.tsx": "Four-step match setup wizard with PlayingElevenSelector prop names corrected to use `players`/`selected`/`onChange` instead of `team`/`selectedPlayerIds`/`onSelectionChange`; Playing11Step now wraps each selector in a Card with the team name as header.",
  "frontend/src/components/PlayingElevenSelector.tsx": "New reusable component that displays a team's full squad as selectable checkboxes with a live counter (e.g., \"7 / 11 selected\"), enforces a maximum of 11 selections, and shows visual feedback for selected/disabled/complete states.",
  "frontend/src/lib/matchStore.ts": "localStorage utilities for stored matches, match meta, and local match state; includes updateStoredMatch helper and clearAllMatchState for the cricket_match_state keys; all bigint values serialized as strings.",
  "frontend/src/components/EndOfOverModal.tsx": "End-of-over modal with solid `bg-background` on `DialogContent` and `bg-black/60 backdrop-blur-sm` on `DialogOverlay` for readable backgrounds; prompts for next bowler selection after every 6 legal balls.",
  "frontend/src/components/WicketModal.tsx": "Wicket modal with solid `bg-background` on `DialogContent` and `bg-black/60 backdrop-blur-sm` on `DialogOverlay` for readable backgrounds; handles dismissal type and next batsman selection.",
  "frontend/src/components/EndOfInningsModal.tsx": "Innings/opener selection modal with solid `bg-background` on `DialogContent` and `bg-black/60 backdrop-blur-sm` on `DialogOverlay` for readable backgrounds; handles striker, non-striker, and bowler selection for innings start.",
  "frontend/src/hooks/useMatchState.ts": "Custom hook managing live match state (striker, non-striker, bowler, innings, match result) with fixed imports — removes non-existent `saveMatchState`/`getMatchState` imports — and uses functional state updates in `swapBatsmen` to avoid stale closure bugs.",
  "frontend/src/components/ResetAllDataModal.tsx": "Confirmation AlertDialog modal for the \"Reset All Data\" action; displays a warning icon, a bulleted list of what will be deleted, and two actions — Cancel and a destructive \"Delete All Data\" button with an inline spinner while the reset is in progress.",
  "frontend/src/pages/Teams.tsx": "Teams management page updated to use QueryErrorState component for the getAllTeams error state, showing a friendly message and Retry button (via refetch) instead of raw IC rejection text.",
  "frontend/src/components/BulkPlayerUploadModal.tsx": "Three-step modal (input → preview → result) for bulk-adding players to a team via pasted text or .txt/.csv file upload; validates names for length, spaces, and duplicates, shows a preview list with skip reasons, then adds all valid players via the addPlayer mutation and displays a summary — the 11-player cap has been removed.",
  "frontend/src/components/ScoreDisplay.tsx": "Score display card showing batting team name, runs/wickets, overs, and for innings 2 shows target, runs required, and innings 1 summary.",
  "frontend/src/components/OverTimeline.tsx": "Over timeline showing last 3 overs with correct 6-ball slots per over; wides and no-balls don't consume legal ball slots but are displayed; empty slots shown as dashed circles.",
  "frontend/src/components/QueryErrorState.tsx": "Reusable error display component that detects IC canister rejection errors (IC0508, IC0503, stopped canister, etc.) and shows a user-friendly message with a WifiOff or AlertCircle icon and an optional Retry button instead of raw rejection text."
}